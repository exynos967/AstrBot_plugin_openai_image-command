{
    "openai_api_key": {
        "description": "OpenAI API 密钥列表",
        "type": "list",
        "hint": "可配置一个或多个 OpenAI API Key；如只需一个可仅填写单项。",
        "default": [],
        "obvious_hint": true
    },
    "openai_api_base": {
        "description": "OpenAI API Base URL",
        "type": "string",
        "hint": "默认为 https://api.openai.com，如使用代理或兼容网关可在此修改。",
        "default": "https://api.openai.com",
        "obvious_hint": false
    },
    "api_format": {
        "description": "后端接口格式",
        "type": "string",
        "hint": "非必要不要使用gemini格式！",
        "default": "openai",
        "options": ["openai", "gemini"],
        "obvious_hint": true
    },
    "model_name": {
        "description": "使用的 OpenAI 图像模型名称",
        "type": "string",
        "hint": "例如：gpt-image-1 或其他支持 /v1/images/generations 的模型",
        "default": "gpt-image-1",
        "obvious_hint": true
    },
    "max_retry_attempts": {
        "description": "OpenAI 图像请求的最大重试次数",
        "type": "int",
        "hint": "当 API 请求失败时最多重试几次。推荐 2-5 次，设置过高可能导致响应时间过长",
        "default": 3,
        "obvious_hint": true
    },
    "nap_server_address": {
        "description": "NAPcat 服务地址，若与服务器在同一服务器上请填写 localhost",
        "type": "string",
        "default": "localhost"
    },
    "nap_server_port": {
        "description": "NAPcat 所处服务器接收文件端口，在同一服务器上可以不填",
        "type": "int",
        "default": 3658
    },
    "group_filter_mode": {
        "description": "群过滤模式",
        "type": "string",
        "hint": "whitelist: 仅名单内群可用；blacklist: 名单内群禁用；none: 不过滤。",
        "default": "none",
        "options": ["none", "whitelist", "blacklist"],
        "obvious_hint": false
    },
    "group_filter_list": {
        "description": "群过滤名单（根据模式作为白名单或黑名单）",
        "type": "list",
        "hint": "填写群号字符串列表。",
        "default": [],
        "obvious_hint": false
    },
    "rate_limit_max_calls_per_group": {
        "description": "单群调用次数上限（每周期）",
        "type": "int",
        "hint": "0 表示不启用限流；大于 0 时，一个群在一个周期内最多可调用的指令次数。",
        "default": 0,
        "obvious_hint": false
    },
    "rate_limit_period_seconds": {
        "description": "限流周期长度（秒）",
        "type": "int",
        "hint": "与单群调用次数上限配合使用，默认为 60 秒。",
        "default": 60,
        "obvious_hint": false
    }
}
